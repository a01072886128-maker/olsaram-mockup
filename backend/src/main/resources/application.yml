spring:
  application:
    name: OlsaramBackend
  datasource:
    url: jdbc:mysql://project-db-campus.smhrd.com:3307/campus_24IS_CLOUD3_p3_4?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: campus_24IS_CLOUD3_p3_4
    password: smhrd4
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: 0
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
      continue-on-error: true

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

# OpenAI API Key Configuration
openai:
  api:
    key: ***REMOVED***

ncloud:
  ocr:
    invoke-url: ${NCLOUD_OCR_INVOKE_URL:https://tdl292xcpf.apigw.ntruss.com/custom/v1/47486/50ee54899ea3cf843286def3afee5338fa12db092e54a3942aa50ec7289b57e8/general}
    api-key-id: ${NCLOUD_OCR_API_KEY_ID:29kzn90k8a}
    api-key: ${NCLOUD_OCR_API_KEY:}
    secret-key: ${NCLOUD_OCR_SECRET_KEY:cENLYmZnTlFJQkdRb1RFTXRKZ21WRExDY2x6dHVKS3A=}

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:olsaram_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: never
